# What Roop does not own

Roop does not own providers, databases, HTTP servers, deployment, user interfaces, sandboxes,
channels, or product-specific tools.

These concerns supply services or host the runtime. They must not become kernel dependencies.
