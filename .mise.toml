[tools]
node = "24.15.0"

[env]
COREPACK_ENABLE_PROJECT_SPEC = "1"

[tasks.install]
description = "Install project dependencies"
run = "corepack pnpm install"

[tasks.check]
description = "Run typecheck, lint, format check, and tests"
run = "corepack pnpm check"
