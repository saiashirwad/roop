name = "roop-edge"
# liminal ActorRuntime worker (RoomRuntime exported as Room)
main = "src/main.ts"
compatibility_date = "2026-07-01"
compatibility_flags = ["nodejs_compat"]

[durable_objects]
bindings = [{ name = "ROOM", class_name = "Room" }]

[[migrations]]
tag = "v1"
new_sqlite_classes = ["Room"]
